# Development Dockerfile with hot reload support
FROM gradle:8.5-jdk21

WORKDIR /app

# Copy Gradle Wrapper
COPY file-service/gradle ./gradle
COPY file-service/gradlew ./gradlew
COPY file-service/gradlew.bat ./gradlew.bat
# RUN chmod +x gradlew

# Copy Gradle configuration files (for better caching)
COPY file-service/build.gradle ./
COPY file-service/gradle.properties ./
COPY file-service/settings.gradle ./
COPY file-service/lombok.config ./

# Download dependencies (cached layer)
RUN ./gradlew dependencies --no-daemon || true

# Copy source code (will be overridden by volume mount in docker-compose)
COPY file-service/src ./src

# Expose port
ARG FILE_SERVICE_PORT=8084
EXPOSE ${FILE_SERVICE_PORT}

# Run with continuous build for hot reload
# Spring Boot DevTools will handle automatic restarts
ENTRYPOINT ["./gradlew", "bootRun", "--no-daemon"]

